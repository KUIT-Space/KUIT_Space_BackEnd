spring:
  datasource:
      url:  jdbc:h2:tcp://localhost/~/kuit_space
      username: sa
      password:
      driver-class-name: org.h2.Driver

  jpa:
    hibernate:
          ddl-auto: create          # table을 전부 drop했다가 다시 생성 -> 테스트용
    properties:
      hibernate:
        format_sql: true
#        show_sql: true

logging:
  level:
    org.hibernate.sql: debug
